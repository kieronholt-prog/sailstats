# Deploy: cd to this directory, then: npx wrangler deploy
# Change `name` if your Worker in Cloudflare uses a different name.
name = "sailstats-auth"
main = "worker.js"
compatibility_date = "2024-11-01"
